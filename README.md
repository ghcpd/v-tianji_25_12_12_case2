# Shopping Cart UI

A modern, frontend-only shopping cart web interface built with React + TypeScript.

Commands:

- Install dependencies: `npm install`
- Run dev server: `npm run dev`
- Run unit tests: `npm test`
- Run e2e tests (Playwright): `npm run e2e`

